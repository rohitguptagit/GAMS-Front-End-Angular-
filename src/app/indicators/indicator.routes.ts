// Imports
import { Routes } from '@angular/router';

import { MajorListComponent }   from './major-list.component';

// Route Configuration
export const indicatorRoutes: Routes = [
  { path: 'indicators', component: MajorListComponent }
];